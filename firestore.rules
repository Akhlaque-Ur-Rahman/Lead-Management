rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Public access allowed because Firebase Auth is removed.
    // In a real production app, you would need a backend to secure this.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
